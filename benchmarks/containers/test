#!/bin/bash

D_COMPILER="ldmd2"              # valid alternatives are dmd, gdc, ldc2 or ldmd2

LDC2=$(type -p $D_COMPILER)
LDC2_DIR=$(dirname "$LDC2")
LDC2_DUB="$LDC2_DIR/dub"

exec "$LDC2_DUB" run --compiler=$D_COMPILER --build=release-nobounds-lto
